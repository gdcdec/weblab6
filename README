              Variant: 16 (2 -- functional, 1 -- improvement)


                                   ABOUT
                                   =====

This repo is related to web development class.


                               INSTALLATION
                               ============

One have to install some software. Following dependencies can be obtained
from package manager or their official site.

    1) git (https://git-scm.com/);
    2) NodeJS (https://nodejs.org/);
    3) PostgreSQL (https://www.postgresql.org/).

To deploy this project one should follow these steps:

    1) Clone `develop' branch of the repository:
        $ git clone -b develop https://github.com/gdcdec/weblab6

    2) Jump to backend directory:
        $ cd weblab6/backend

    3) Intall NodeJS dependencies specified in package.json:
        $ npm install --omit=dev

       or just

        $ npm install

       `--omit=dev' flag is required to parry installation of packages
       specified in `devDependencies' section of package.json. By default,
       `npm install' installs all modules listed both in `dependecies' and
       `devDependencies' of package.json.

    4) Create `.env' file inside backend directory:

        DB_NAME=
        DB_USER=
        DB_PASSWORD=
        DB_HOST=
        DB_PORT=
        DATABASE_DIALECT=postgres

       Fill in the right sides with necessary values.

    5) Create database with the same name as mentioned in `.env' file.

    6) Run server:
        $ npm run dev

       to run server in development mode with `nodemon' (`nodemon' won't be
       installed with `npm install --omit=dev') or

        $ npm run


                                   USAGE
                                   =====

Graphical user interface is not implemented yet. To obtain current functionality
one can use curl to make a request to the server:

    * Get test response:
        $ curl -X GET localhost:3000

    * Get all users:
        $ curl -X GET -H "Content-Type: application/json" -H "Accept: application/json" http://localhost:3000/users

    * Create new user:
        $ curl -X POST -d '{ "name": "John Smith", "email": "john.smith@example.com" }' -H "Content-Type: application/json" http://localhost:3000/users

    * Get all events:
        $ curl -X GET -H "Content-Type: application/json" -H "Accept: application/json" http://localhost:3000/events

    * Get event by its id:
        $ curl -X GET -H "Content-Type: application/json" -H "Accept: application/json" http://localhost:3000/events/id/<eventId>

    * Get events with specific category:
        $ curl -X GET -H "Content-Type: application/json" -H "Accept: application/json" http://localhost:3000/events/cat/<eventCat>

      Only categories listed in 'backend/src/database/model/eventModel.js'
      in 'categories' enumeration are permitted. Values of enumeration are
      case sensitive.

    * Create new event:
        $ curl -X POST -d '{ "title": "Event with category", "description": "Description for new event with category", "date": "2025-03-15T14:00:00.000Z", "createdBy": 1, "category": "amusement" }' -H "Content-Type: application/json" http://localhost:3000/events

    * Update event by its id:
        $ curl -X PUT -d '{ "title": "Updated Event", "description": "Description for updated event", "date": "2025-12-12T14:00:00.000Z", "createdBy": 1, "category": "education" }' -H "Content-Type: application/json" http://localhost:3000/events/<eventID>

    * Delete event by its id:
        $ curl -X DELETE -H "Content-Type: application/json" http://localhost:3000/events/<eventID>
